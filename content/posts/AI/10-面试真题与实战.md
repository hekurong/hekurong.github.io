---
title: "面试真题与实战"
date: 2026-06-01
categories: ["AI"]
tags: ["面试", "真题", "实战"]
series: "AI应用开发面试宝典"
toc: true
weight: 1
ai_generated: true
---

# 面试真题与实战

## 概述

本章汇集AI应用开发面试中常见的笔试、编程题、系统设计题和行为面试题。内容覆盖从基础算法实现到完整RAG/Agent系统搭建的编码实战，以及各公司面经和考前冲刺清单，是面试前冲刺复习的核心资料。

## 知识点详解

### 一、基础算法题

#### 1. Attention机制实现

```python
import numpy as np

def scaled_dot_product_attention(Q, K, V, mask=None):
    """
    Scaled Dot-Product Attention 实现
    
    Args:
        Q: Query矩阵, shape (batch_size, seq_len, d_k)
        K: Key矩阵, shape (batch_size, seq_len, d_k)
        V: Value矩阵, shape (batch_size, seq_len, d_v)
        mask: 可选mask, shape (batch_size, seq_len, seq_len)
    
    Returns:
        output: Attention输出
        attention_weights: 注意力权重
    """
    d_k = Q.shape[-1]
    
    # 1. 计算QK^T / sqrt(d_k)
    scores = np.matmul(Q, K.transpose(0, 2, 1)) / np.sqrt(d_k)
    
    # 2. 应用mask（如果有）
    if mask is not None:
        scores = np.where(mask == 0, -1e9, scores)
    
    # 3. Softmax
    exp_scores = np.exp(scores - np.max(scores, axis=-1, keepdims=True))
    attention_weights = exp_scores / np.sum(exp_scores, axis=-1, keepdims=True)
    
    # 4. 加权求和
    output = np.matmul(attention_weights, V)
    
    return output, attention_weights


def multi_head_attention(Q, K, V, num_heads, d_model, mask=None):
    """
    多头注意力实现
    
    Args:
        Q, K, V: 输入矩阵
        num_heads: 注意力头数
        d_model: 模型维度
    """
    batch_size = Q.shape[0]
    d_k = d_model // num_heads
    
    # 1. 线性变换并分头
    def split_heads(x):
        x = x.reshape(batch_size, -1, num_heads, d_k)
        return x.transpose(0, 2, 1, 3)  # (batch, heads, seq, d_k)
    
    Q_heads = split_heads(Q)
    K_heads = split_heads(K)
    V_heads = split_heads(V)
    
    # 2. 对每个头计算attention
    output_heads, attn_weights = scaled_dot_product_attention(
        Q_heads, K_heads, V_heads, mask
    )
    
    # 3. 合并头
    output = output_heads.transpose(0, 2, 1, 3)  # (batch, seq, heads, d_k)
    output = output.reshape(batch_size, -1, d_model)
    
    return output


# 测试
batch_size, seq_len, d_model = 2, 4, 8
num_heads = 2
Q = np.random.randn(batch_size, seq_len, d_model)
K = np.random.randn(batch_size, seq_len, d_model)
V = np.random.randn(batch_size, seq_len, d_model)

output = multi_head_attention(Q, K, V, num_heads, d_model)
print(f"Multi-Head Attention Output shape: {output.shape}")
```

#### 2. Top-K Sampling 实现

```python
import numpy as np

def top_k_sampling(logits, k=50, temperature=1.0):
    """
    Top-K采样实现
    
    Args:
        logits: 模型输出logits, shape (vocab_size,)
        k: 保留概率最高的k个token
        temperature: 温度参数（>1更随机，<1更确定）
    
    Returns:
        sampled_token: 采样得到的token id
    """
    # 1. 应用温度
    logits = logits / temperature
    
    # 2. 取Top-K
    top_k_values = -np.sort(-logits)[:k]
    top_k_indices = np.argsort(-logits)[:k]
    threshold = top_k_values[-1]
    
    # 3. 将小于阈值的logits设为负无穷
    filtered_logits = np.where(
        logits >= threshold, logits, -float('Inf')
    )
    
    # 4. Softmax转换为概率
    exp_logits = np.exp(filtered_logits - np.max(filtered_logits))
    probs = exp_logits / np.sum(exp_logits)
    
    # 5. 根据概率采样
    sampled_token = np.random.choice(len(probs), p=probs)
    
    return sampled_token


def top_p_sampling(logits, p=0.9, temperature=1.0):
    """
    Top-P (Nucleus) 采样实现
    """
    logits = logits / temperature
    sorted_indices = np.argsort(logits)[::-1]
    sorted_logits = logits[sorted_indices]
    
    # 计算累积概率
    exp_logits = np.exp(sorted_logits - np.max(sorted_logits))
    probs = exp_logits / np.sum(exp_logits)
    cum_probs = np.cumsum(probs)
    
    # 找到累积概率超过p的阈值
    threshold_idx = np.where(cum_probs > p)[0][0]
    threshold = sorted_logits[threshold_idx]
    
    # 过滤
    filtered_logits = np.where(logits >= threshold, logits, -float('Inf'))
    exp_logits = np.exp(filtered_logits - np.max(filtered_logits))
    probs = exp_logits / np.sum(exp_logits)
    
    return np.random.choice(len(probs), p=probs)
```

#### 3. KV Cache 模拟

```python
import numpy as np

class KVCache:
    """KV Cache模拟实现"""
    
    def __init__(self, max_batch_size=1, max_seq_len=4096, num_layers=32,
                 num_heads=32, head_dim=128):
        self.max_batch_size = max_batch_size
        self.max_seq_len = max_seq_len
        self.num_layers = num_layers
        self.num_heads = num_heads
        self.head_dim = head_dim
        
        # 初始化KV Cache（预分配内存）
        self.k_cache = np.zeros(
            (num_layers, max_batch_size, num_heads, max_seq_len, head_dim),
            dtype=np.float16
        )
        self.v_cache = np.zeros_like(self.k_cache)
        self.current_seq_len = 0
    
    def update(self, layer_idx, k_new, v_new):
        """
        更新KV Cache（追加新的K、V）
        
        Args:
            layer_idx: 层索引
            k_new: 新的Key, shape (batch, heads, new_tokens, dim)
            v_new: 新的Value, shape (batch, heads, new_tokens, dim)
        """
        new_tokens = k_new.shape[-2]
        start = self.current_seq_len
        end = start + new_tokens
        
        self.k_cache[layer_idx, :, :, start:end, :] = k_new
        self.v_cache[layer_idx, :, :, start:end, :] = v_new
        
        self.current_seq_len = end
    
    def get(self, layer_idx):
        """获取当前缓存的K、V"""
        return (
            self.k_cache[layer_idx, :, :, :self.current_seq_len, :],
            self.v_cache[layer_idx, :, :, :self.current_seq_len, :]
        )
    
    def reset(self):
        """重置Cache（新序列时调用）"""
        self.current_seq_len = 0
    
    def memory_usage_gb(self):
        """计算KV Cache显存占用（GB）"""
        bytes_per_element = 2  # float16
        total_elements = self.k_cache.size + self.v_cache.size
        return total_elements * bytes_per_element / (1024 ** 3)


# 模拟自回归生成
class AutoregressiveGenerator:
    def __init__(self, vocab_size=32000, d_model=4096, num_layers=32,
                 num_heads=32):
        self.vocab_size = vocab_size
        self.d_model = d_model
        self.num_layers = num_layers
        self.num_heads = num_heads
        self.head_dim = d_model // num_heads
        self.kv_cache = KVCache(
            num_layers=num_layers,
            num_heads=num_heads,
            head_dim=self.head_dim
        )
    
    def generate(self, input_ids, max_new_tokens=100):
        """模拟带KV Cache的自回归生成"""
        generated = list(input_ids)
        
        # Prefill阶段：处理所有输入token
        for pos, token_id in enumerate(input_ids):
            # 模拟单token的KV计算
            k = np.random.randn(1, self.num_heads, 1, self.head_dim).astype(np.float16)
            v = np.random.randn(1, self.num_heads, 1, self.head_dim).astype(np.float16)
            
            # 更新KV Cache
            for layer in range(self.num_layers):
                self.kv_cache.update(layer, k, v)
        
        # Decode阶段：逐token生成
        for _ in range(max_new_tokens):
            # 只输入最后一个token
            last_token = generated[-1]
            
            # 计算当前token的K、V
            k_new = np.random.randn(1, self.num_heads, 1, self.head_dim).astype(np.float16)
            v_new = np.random.randn(1, self.num_heads, 1, self.head_dim).astype(np.float16)
            
            # 更新KV Cache（追加）
            for layer in range(self.num_layers):
                self.kv_cache.update(layer, k_new, v_new)
            
            # 模拟预测下一个token
            logits = np.random.randn(self.vocab_size)
            next_token = np.argmax(logits)
            generated.append(next_token)
            
            if next_token == 0:  # EOS token
                break
        
        return generated


# 演示
gen = AutoregressiveGenerator()
output = gen.generate([1, 2, 3, 4, 5], max_new_tokens=10)
print(f"Generated tokens: {output}")
print(f"KV Cache memory: {gen.kv_cache.memory_usage_gb():.2f} GB")
```

### 二、RAG实现题

#### 完整RAG代码实现

```python
"""
完整RAG系统实现：文档加载 -> 切分 -> 向量化 -> 检索 -> 生成

依赖安装：
pip install langchain langchain-community chromadb sentence-transformers
pip install pypdf openai tiktoken
"""

import os
from typing import List, Optional, Tuple
import hashlib
import json

# ============ 1. 文档加载 ============

from langchain_community.document_loaders import (
    PyPDFLoader,
    TextLoader,
    DirectoryLoader,
    WebBaseLoader,
)
from langchain_core.documents import Document

class DocumentLoader:
    """多格式文档加载器"""
    
    SUPPORTED_EXTENSIONS = {
        ".pdf": PyPDFLoader,
        ".txt": TextLoader,
        ".md": TextLoader,
    }
    
    @classmethod
    def load_file(cls, file_path: str) -> List[Document]:
        ext = os.path.splitext(file_path)[1].lower()
        loader_class = cls.SUPPORTED_EXTENSIONS.get(ext)
        if not loader_class:
            raise ValueError(f"Unsupported file type: {ext}")
        
        loader = loader_class(file_path)
        return loader.load()
    
    @classmethod
    def load_directory(cls, dir_path: str) -> List[Document]:
        docs = []
        for ext, loader_class in cls.SUPPORTED_EXTENSIONS.items():
            loader = DirectoryLoader(
                dir_path,
                glob=f"**/*{ext}",
                loader_cls=loader_class,
                show_progress=True
            )
            docs.extend(loader.load())
        return docs
    
    @classmethod
    def load_webpage(cls, url: str) -> List[Document]:
        loader = WebBaseLoader(url)
        return loader.load()


# ============ 2. 文本切分 ============

from langchain_text_splitters import (
    RecursiveCharacterTextSplitter,
    TokenTextSplitter,
)

class TextChunker:
    """文本切分器"""
    
    @staticmethod
    def recursive_chunk(
        documents: List[Document],
        chunk_size: int = 500,
        chunk_overlap: int = 50,
    ) -> List[Document]:
        """递归字符切分（推荐）"""
        splitter = RecursiveCharacterTextSplitter(
            chunk_size=chunk_size,
            chunk_overlap=chunk_overlap,
            separators=["\n\n", "\n", "。", ".", " ", ""],
            length_function=len,
        )
        return splitter.split_documents(documents)
    
    @staticmethod
    def token_chunk(
        documents: List[Document],
        chunk_size: int = 300,
        chunk_overlap: int = 30,
    ) -> List[Document]:
        """基于Token的切分"""
        splitter = TokenTextSplitter(
            chunk_size=chunk_size,
            chunk_overlap=chunk_overlap,
        )
        return splitter.split_documents(documents)
    
    @staticmethod
    def semantic_chunk(
        documents: List[Document],
        max_chunk_size: int = 500,
        min_chunk_size: int = 100,
    ) -> List[Document]:
        """
        语义切分：按段落边界切分，尽量保持语义完整
        适合文档结构清晰的场景
        """
        chunks = []
        for doc in documents:
            paragraphs = doc.page_content.split("\n\n")
            current_chunk = []
            current_size = 0
            
            for para in paragraphs:
                para_size = len(para)
                if current_size + para_size > max_chunk_size and current_chunk:
                    chunks.append(Document(
                        page_content="\n\n".join(current_chunk),
                        metadata=doc.metadata
                    ))
                    current_chunk = [para]
                    current_size = para_size
                else:
                    current_chunk.append(para)
                    current_size += para_size
            
            if current_chunk:
                chunks.append(Document(
                    page_content="\n\n".join(current_chunk),
                    metadata=doc.metadata
                ))
        
        return chunks


# ============ 3. 向量化与存储 ============

from langchain_openai import OpenAIEmbeddings
from langchain_community.embeddings import HuggingFaceEmbeddings
from langchain_community.vectorstores import Chroma

class VectorStore:
    """向量存储管理"""
    
    def __init__(
        self,
        embedding_model: str = "BAAI/bge-small-zh-v1.5",
        persist_directory: str = "./chroma_db",
    ):
        self.embedding_model = embedding_model
        self.persist_directory = persist_directory
        
        # 初始化Embedding模型
        self.embeddings = HuggingFaceEmbeddings(
            model_name=embedding_model,
            model_kwargs={"device": "cpu"},
            encode_kwargs={"normalize_embeddings": True},
        )
        
        # 初始化或加载向量库
        self.vectorstore = None
        if os.path.exists(persist_directory):
            self.vectorstore = Chroma(
                persist_directory=persist_directory,
                embedding_function=self.embeddings,
            )
    
    def create_index(self, chunks: List[Document]):
        """创建向量索引"""
        # 为每个chunk添加唯一ID
        for i, chunk in enumerate(chunks):
            chunk.metadata["chunk_id"] = hashlib.md5(
                chunk.page_content.encode()
            ).hexdigest()[:12]
        
        self.vectorstore = Chroma.from_documents(
            documents=chunks,
            embedding=self.embeddings,
            persist_directory=self.persist_directory,
        )
        self.vectorstore.persist()
        print(f"Created index with {len(chunks)} chunks")
    
    def similarity_search(
        self, query: str, k: int = 5
    ) -> List[Document]:
        """向量相似度检索"""
        if not self.vectorstore:
            raise ValueError("Vector store not initialized")
        return self.vectorstore.similarity_search(query, k=k)
    
    def similarity_search_with_score(
        self, query: str, k: int = 5
    ) -> List[Tuple[Document, float]]:
        """带分数的相似度检索"""
        if not self.vectorstore:
            raise ValueError("Vector store not initialized")
        return self.vectorstore.similarity_search_with_score(query, k=k)


# ============ 4. 检索与生成 ============

from openai import OpenAI
import numpy as np

class RAGSystem:
    """完整RAG系统"""
    
    def __init__(
        self,
        embedding_model: str = "BAAI/bge-small-zh-v1.5",
        llm_endpoint: str = "http://localhost:8000/v1",
        llm_model: str = "qwen2.5-7b",
        persist_directory: str = "./chroma_db",
    ):
        # 向量存储
        self.vector_store = VectorStore(
            embedding_model=embedding_model,
            persist_directory=persist_directory,
        )
        
        # LLM客户端
        self.llm_client = OpenAI(
            base_url=llm_endpoint,
            api_key="not-needed"
        )
        self.llm_model = llm_model
        
        # 检索设置
        self.top_k = 5
        self.score_threshold = 0.7
    
    def index_documents(self, docs: List[Document]):
        """索引文档"""
        chunks = TextChunker.recursive_chunk(docs)
        self.vector_store.create_index(chunks)
    
    def retrieve(self, query: str) -> List[Document]:
        """检索相关文档"""
        results = self.vector_store.similarity_search_with_score(
            query, k=self.top_k
        )
        # 按分数阈值过滤
        relevant_docs = [
            doc for doc, score in results
            if score >= self.score_threshold
        ] if self.score_threshold else [doc for doc, _ in results]
        return relevant_docs
    
    def generate(self, query: str, context_docs: List[Document]) -> str:
        """基于检索结果生成回答"""
        context = "\n\n".join([
            f"[文档{i+1}] {doc.page_content}"
            for i, doc in enumerate(context_docs)
        ])
        
        system_prompt = """你是一个专业的AI助手，请基于以下提供的上下文准确回答用户问题。
如果上下文中没有足够信息，请明确说明"根据提供的资料无法回答此问题"。
请用中文回答，并引用相关文档编号。"""
        
        user_prompt = f"""上下文：
{context}

用户问题：{query}

请基于上下文回答问题："""
        
        response = self.llm_client.chat.completions.create(
            model=self.llm_model,
            messages=[
                {"role": "system", "content": system_prompt},
                {"role": "user", "content": user_prompt},
            ],
            temperature=0.3,
            max_tokens=1024,
        )
        
        return response.choices[0].message.content
    
    def query(self, query: str) -> dict:
        """完整RAG查询流程"""
        # 1. 检索
        retrieved_docs = self.retrieve(query)
        
        if not retrieved_docs:
            return {
                "query": query,
                "answer": "未检索到相关文档。",
                "sources": [],
                "has_context": False,
            }
        
        # 2. 生成
        answer = self.generate(query, retrieved_docs)
        
        # 3. 返回结果
        sources = [
            {
                "content": doc.page_content[:200] + "...",
                "source": doc.metadata.get("source", "unknown"),
            }
            for doc in retrieved_docs[:3]
        ]
        
        return {
            "query": query,
            "answer": answer,
            "sources": sources,
            "has_context": True,
        }


# ============ 使用示例 ============

if __name__ == "__main__":
    # 初始化RAG系统
    rag = RAGSystem()
    
    # 1. 加载文档
    print("Loading documents...")
    docs = DocumentLoader.load_directory("./documents")
    print(f"Loaded {len(docs)} documents")
    
    # 2. 索引文档
    print("Indexing documents...")
    rag.index_documents(docs)
    
    # 3. 查询
    result = rag.query("什么是模型量化？")
    print(f"\n查询: {result['query']}")
    print(f"回答: {result['answer']}")
    print(f"来源: {result['sources']}")
```

### 三、Agent实现题

#### 1. ReAct Agent 实现

```python
"""
ReAct (Reasoning + Acting) Agent 实现
思想：将推理步骤和工具调用交替进行
"""

import json
import re
from typing import Callable, Dict, List, Any
from openai import OpenAI


class Tool:
    """工具定义"""
    
    def __init__(self, name: str, description: str, func: Callable,
                 parameters: dict):
        self.name = name
        self.description = description
        self.func = func
        self.parameters = parameters
    
    def to_openai_tool(self) -> dict:
        return {
            "type": "function",
            "function": {
                "name": self.name,
                "description": self.description,
                "parameters": self.parameters,
            }
        }
    
    def execute(self, **kwargs) -> str:
        try:
            result = self.func(**kwargs)
            return str(result)
        except Exception as e:
            return f"Error: {str(e)}"


class ReActAgent:
    """
    ReAct Agent实现
    
    工作流程：
    1. 思考（Thought）：决定下一步做什么
    2. 行动（Action）：执行工具调用
    3. 观察（Observation）：获取工具执行结果
    4. 循环直到得到最终答案
    """
    
    def __init__(self, tools: List[Tool], model: str = "gpt-4",
                 max_steps: int = 10):
        self.tools = {tool.name: tool for tool in tools}
        self.tool_definitions = [t.to_openai_tool() for t in tools]
        self.model = model
        self.max_steps = max_steps
        self.client = OpenAI()
    
    def run(self, user_input: str) -> str:
        """运行ReAct Agent"""
        messages = [
            {"role": "system", "content": self._system_prompt()},
            {"role": "user", "content": user_input},
        ]
        
        steps = []
        
        for step in range(self.max_steps):
            print(f"\n=== Step {step + 1} ===")
            
            # 调用LLM
            response = self.client.chat.completions.create(
                model=self.model,
                messages=messages,
                tools=self.tool_definitions,
                tool_choice="auto",
                temperature=0.7,
            )
            
            assistant_message = response.choices[0].message
            
            # 检查是否有工具调用
            if not assistant_message.tool_calls:
                # 没有工具调用 -> 最终答案
                final_answer = assistant_message.content
                print(f"Final answer: {final_answer}")
                return final_answer
            
            # 处理工具调用
            messages.append(assistant_message)
            
            for tool_call in assistant_message.tool_calls:
                tool_name = tool_call.function.name
                arguments = json.loads(tool_call.function.arguments)
                
                print(f"Thought: 需要调用 {tool_name}")
                print(f"Action: {tool_name}({arguments})")
                
                # 执行工具
                if tool_name in self.tools:
                    result = self.tools[tool_name].execute(**arguments)
                else:
                    result = f"Error: Tool {tool_name} not found"
                
                print(f"Observation: {result[:200]}")
                
                # 添加工具响应
                messages.append({
                    "role": "tool",
                    "tool_call_id": tool_call.id,
                    "content": result,
                })
                
                steps.append({
                    "step": step + 1,
                    "tool": tool_name,
                    "args": arguments,
                    "result": result[:200],
                })
        
        return "Max steps reached without final answer."
    
    def _system_prompt(self) -> str:
        tool_descriptions = "\n".join([
            f"- {tool.name}: {tool.description}"
            for tool in self.tools.values()
        ])
        return f"""你是一个使用工具的AI助手。请按照以下格式工作：

可用工具：
{tool_descriptions}

工作流程：
1. 分析用户问题，思考需要使用哪些工具
2. 调用合适的工具获取信息
3. 根据工具返回结果继续思考
4. 当获取足够信息后，给出最终答案

注意：
- 每次只调用一个工具
- 调用工具时使用精确的参数
- 如果工具返回错误，尝试其他方法
- 重要：当不再需要调用工具时，直接回复最终答案"""


# ============ 工具定义示例 ============

def search_web(query: str) -> str:
    """模拟网络搜索"""
    database = {
        "北京天气": "北京今天晴，25-30°C",
        "上海天气": "上海今天多云，28-33°C",
        "Python": "Python是一种高级编程语言",
        "RAG": "RAG是Retrieval-Augmented Generation的缩写",
    }
    return database.get(query, f"未找到'{query}'的相关结果")


def calculate(expression: str) -> str:
    """数学计算"""
    try:
        # 安全计算
        allowed_names = {"abs": abs, "round": round, "int": int, "float": float}
        result = eval(expression, {"__builtins__": {}}, allowed_names)
        return f"计算结果: {result}"
    except Exception as e:
        return f"计算错误: {str(e)}"


# 定义工具
tools = [
    Tool(
        name="search",
        description="搜索网络获取信息",
        func=search_web,
        parameters={
            "type": "object",
            "properties": {
                "query": {"type": "string", "description": "搜索关键词"}
            },
            "required": ["query"]
        }
    ),
    Tool(
        name="calculate",
        description="数学计算",
        func=calculate,
        parameters={
            "type": "object",
            "properties": {
                "expression": {"type": "string", "description": "数学表达式"}
            },
            "required": ["expression"]
        }
    ),
]


# 运行Agent
if __name__ == "__main__":
    agent = ReActAgent(tools=tools)
    result = agent.run("北京今天多少度？比上海高还是低？")
    print(f"\n最终回答: {result}")
```


#### 2. Function Calling 回调实现

```python
"""
Function Calling 的回调实现
支持同步和异步回调、超时控制、错误处理
"""

import asyncio
import functools
import time
from typing import Any, Callable, Dict, Optional
from dataclasses import dataclass


@dataclass
class FunctionCall:
    """函数调用封装"""
    name: str
    arguments: Dict[str, Any]
    call_id: str
    timestamp: float


@dataclass
class FunctionResult:
    """函数调用结果"""
    call_id: str
    name: str
    result: Any
    success: bool
    error: Optional[str] = None
    duration_ms: float = 0.0


class FunctionCallingHandler:
    """
    函数调用处理器
    
    功能：
    - 注册和管理函数
    - 执行函数调用（同步/异步）
    - 超时控制
    - 错误处理和重试
    - 调用日志记录
    """
    
    def __init__(self, timeout: float = 30.0, max_retries: int = 2):
        self._functions: Dict[str, Callable] = {}
        self._definitions: list = []
        self.timeout = timeout
        self.max_retries = max_retries
        self.call_log: list = []
    
    def register(self, func: Callable = None, *, name: str = None,
                 description: str = None, parameters: dict = None):
        """
        注册函数（支持装饰器用法）
        
        @handler.register
        def my_func(...): ...
        
        @handler.register(name="custom_name", ...)
        def my_func(...): ...
        """
        if func is None:
            return functools.partial(
                self.register, name=name,
                description=description, parameters=parameters
            )
        
        func_name = name or func.__name__
        func_description = description or (func.__doc__ or "").strip()
        
        self._functions[func_name] = func
        self._definitions.append({
            "type": "function",
            "function": {
                "name": func_name,
                "description": func_description,
                "parameters": parameters or {
                    "type": "object",
                    "properties": {},
                    "required": [],
                },
            },
        })
        
        return func
    
    async def execute_async(self, call: FunctionCall) -> FunctionResult:
        """异步执行函数调用"""
        start_time = time.time()
        
        if call.name not in self._functions:
            return FunctionResult(
                call_id=call.call_id,
                name=call.name,
                result=None,
                success=False,
                error=f"Function '{call.name}' not registered",
                duration_ms=(time.time() - start_time) * 1000,
            )
        
        func = self._functions[call.name]
        
        for attempt in range(self.max_retries + 1):
            try:
                if asyncio.iscoroutinefunction(func):
                    result = await asyncio.wait_for(
                        func(**call.arguments),
                        timeout=self.timeout,
                    )
                else:
                    result = await asyncio.wait_for(
                        asyncio.to_thread(func, **call.arguments),
                        timeout=self.timeout,
                    )
                
                fr = FunctionResult(
                    call_id=call.call_id,
                    name=call.name,
                    result=result,
                    success=True,
                    duration_ms=(time.time() - start_time) * 1000,
                )
                
                self.call_log.append(fr)
                return fr
                
            except asyncio.TimeoutError:
                error = f"Function '{call.name}' timed out after {self.timeout}s"
                if attempt < self.max_retries:
                    print(f"{error}, retrying ({attempt + 1}/{self.max_retries})")
                    continue
                fr = FunctionResult(
                    call_id=call.call_id,
                    name=call.name,
                    result=None,
                    success=False,
                    error=error,
                    duration_ms=(time.time() - start_time) * 1000,
                )
                self.call_log.append(fr)
                return fr
                
            except Exception as e:
                error = f"Function '{call.name}' failed: {str(e)}"
                if attempt < self.max_retries:
                    print(f"{error}, retrying ({attempt + 1}/{self.max_retries})")
                    continue
                fr = FunctionResult(
                    call_id=call.call_id,
                    name=call.name,
                    result=None,
                    success=False,
                    error=error,
                    duration_ms=(time.time() - start_time) * 1000,
                )
                self.call_log.append(fr)
                return fr
    
    async def execute_all(self, calls: list) -> list:
        """批量并行执行多个函数调用"""
        tasks = [self.execute_async(call) for call in calls]
        return await asyncio.gather(*tasks)
    
    @property
    def definitions(self) -> list:
        """获取OpenAI格式的工具定义"""
        return self._definitions
    
    def get_call_stats(self) -> dict:
        """获取调用统计"""
        if not self.call_log:
            return {}
        
        total = len(self.call_log)
        success = sum(1 for c in self.call_log if c.success)
        avg_duration = sum(c.duration_ms for c in self.call_log) / total
        
        return {
            "total_calls": total,
            "success_rate": f"{success / total * 100:.1f}%",
            "avg_duration_ms": f"{avg_duration:.1f}",
        }


# ============ 使用示例 ============

import random

handler = FunctionCallingHandler(timeout=10.0)

@handler.register
def get_weather(city: str) -> dict:
    """获取指定城市的天气"""
    temperatures = {"北京": 28, "上海": 31, "深圳": 33}
    return {
        "city": city,
        "temperature": temperatures.get(city, random.randint(15, 35)),
        "condition": random.choice(["晴", "多云", "阴"]),
    }

@handler.register(name="send_email")
async def send_email_async(to: str, subject: str, body: str) -> str:
    """发送邮件"""
    await asyncio.sleep(1)  # 模拟网络延迟
    return f"Email sent to {to}: {subject}"

# 使用
async def demo():
    # 获取工具定义
    print("Tool definitions:", json.dumps(handler.definitions, indent=2, ensure_ascii=False))
    
    # 执行函数调用
    call = FunctionCall(
        name="get_weather",
        arguments={"city": "北京"},
        call_id="call_001",
        timestamp=time.time(),
    )
    
    result = await handler.execute_async(call)
    print(f"Result: {result}")
    
    # 批量执行
    calls = [
        FunctionCall(name="get_weather", arguments={"city": "上海"}, call_id="call_002", timestamp=time.time()),
        FunctionCall(name="send_email", arguments={"to": "test@example.com", "subject": "Hello", "body": "World"}, call_id="call_003", timestamp=time.time()),
    ]
    results = await handler.execute_all(calls)
    for r in results:
        print(f"  {r.name}: {'OK' if r.success else 'FAIL'}")
    
    # 统计
    print("Stats:", handler.get_call_stats())

# asyncio.run(demo())
```

### 四、微调实操题

#### LoRA 训练脚本

```python
"""
完整LoRA训练脚本
使用HuggingFace Transformers + PEFT + TRL
"""

import os
import torch
from datasets import Dataset, load_dataset
from transformers import (
    AutoModelForCausalLM,
    AutoTokenizer,
    TrainingArguments,
    BitsAndBytesConfig,
)
from peft import (
    LoraConfig,
    get_peft_model,
    prepare_model_for_kbit_training,
    TaskType,
)
from trl import SFTTrainer


def setup_lora_training(
    model_name: str = "Qwen/Qwen2.5-7B-Instruct",
    dataset_path: str = "./data/train.jsonl",
    output_dir: str = "./lora_output",
    use_4bit: bool = True,
):
    """
    配置LoRA训练
    
    Args:
        model_name: 基础模型
        dataset_path: 训练数据路径
        output_dir: 输出目录
        use_4bit: 是否使用4bit量化（节省显存）
    """
    
    # ============ 1. 量化配置（节省显存） ============
    
    bnb_config = None
    if use_4bit:
        bnb_config = BitsAndBytesConfig(
            load_in_4bit=True,
            bnb_4bit_quant_type="nf4",
            bnb_4bit_compute_dtype=torch.bfloat16,
            bnb_4bit_use_double_quant=True,
        )
    
    # ============ 2. 加载模型和Tokenizer ============
    
    model = AutoModelForCausalLM.from_pretrained(
        model_name,
        quantization_config=bnb_config,
        device_map="auto",
        trust_remote_code=True,
        torch_dtype=torch.bfloat16,
    )
    
    tokenizer = AutoTokenizer.from_pretrained(
        model_name,
        trust_remote_code=True,
        padding_side="right",
    )
    
    if tokenizer.pad_token is None:
        tokenizer.pad_token = tokenizer.eos_token
    
    # 准备k-bit训练
    if use_4bit:
        model = prepare_model_for_kbit_training(model)
    
    # ============ 3. LoRA配置 ============
    
    lora_config = LoraConfig(
        r=16,                      # LoRA秩
        lora_alpha=32,             # LoRA缩放因子
        target_modules=[           # 目标模块
            "q_proj", "k_proj", "v_proj", "o_proj",
            "gate_proj", "up_proj", "down_proj",
        ],
        lora_dropout=0.05,         # Dropout
        bias="none",               # 偏置
        task_type=TaskType.CAUSAL_LM,
    )
    
    model = get_peft_model(model, lora_config)
    model.print_trainable_parameters()  # 打印可训练参数数量
    
    # ============ 4. 准备数据集 ============
    
    dataset = load_dataset("json", data_files=dataset_path, split="train")
    
    def formatting_func(example):
        """格式化数据为对话格式"""
        if "messages" in example:
            # OpenAI格式 [{role: "user", content: "..."}, ...]
            text = tokenizer.apply_chat_template(
                example["messages"],
                tokenize=False,
                add_generation_prompt=False,
            )
        elif "instruction" in example:
            # Alpaca格式
            text = f"""### Instruction:
{example['instruction']}

### Input:
{example.get('input', '')}

### Response:
{example['output']}"""
        else:
            text = example["text"]
        return text
    
    # ============ 5. 训练参数 ============
    
    training_args = TrainingArguments(
        output_dir=output_dir,
        per_device_train_batch_size=4,
        gradient_accumulation_steps=4,
        num_train_epochs=3,
        learning_rate=2e-4,
        fp16=False,
        bf16=True,
        logging_steps=10,
        save_steps=100,
        save_total_limit=3,
        remove_unused_columns=False,
        report_to="none",  # 不使用wandb等
        optim="paged_adamw_8bit",
        lr_scheduler_type="cosine",
        warmup_ratio=0.03,
        max_grad_norm=0.3,
        group_by_length=True,
    )
    
    # ============ 6. 训练器 ============
    
    trainer = SFTTrainer(
        model=model,
        tokenizer=tokenizer,
        args=training_args,
        train_dataset=dataset,
        max_seq_length=2048,
        formatting_func=formatting_func,
        dataset_text_field="text",
        packing=False,
    )
    
    # ============ 7. 开始训练 ============
    
    trainer.train()
    
    # ============ 8. 保存模型 ============
    
    trainer.save_model(output_dir)
    tokenizer.save_pretrained(output_dir)
    
    print(f"LoRA模型已保存到: {output_dir}")
    
    return model, tokenizer


# ============ 数据集格式转换 ============

def convert_to_sharegpt_format(data: list) -> list:
    """
    转换为ShareGPT格式（训练通用格式）
    
    输入格式: [{"question": "...", "answer": "..."}, ...]
    输出格式: [{"messages": [{"role": "user", "content": "..."}, {"role": "assistant", "content": "..."}]}, ...]
    """
    converted = []
    for item in data:
        converted.append({
            "messages": [
                {"role": "user", "content": item["question"]},
                {"role": "assistant", "content": item["answer"]},
            ]
        })
    return converted


def convert_to_alpaca_format(data: list) -> list:
    """
    转换为Alpaca格式
    """
    converted = []
    for item in data:
        converted.append({
            "instruction": item["instruction"],
            "input": item.get("input", ""),
            "output": item["output"],
        })
    return converted


def convert_from_csv(csv_path: str, output_path: str):
    """
    从CSV转换为训练数据
    """
    import pandas as pd
    df = pd.read_csv(csv_path)
    converted = []
    for _, row in df.iterrows():
        converted.append({
            "messages": [
                {"role": "user", "content": row["prompt"]},
                {"role": "assistant", "content": row["completion"]},
            ]
        })
    
    import json
    with open(output_path, "w", encoding="utf-8") as f:
        for item in converted:
            f.write(json.dumps(item, ensure_ascii=False) + "\n")
    
    print(f"转换完成，共 {len(converted)} 条数据")
```


### 五、Prompt实战题

#### 1. 多轮对话Prompt设计

```python
# ============ 多轮对话Prompt模板 ============

SYSTEM_PROMPT_TEMPLATE = """你是一个{role}，擅长{expertise}。

【对话规范】
1. 保持{personality}的语气
2. 每次回答控制在{max_length}字以内
3. 如果用户的问题超出知识范围，请明确说明
4. {extra_rules}

【当前时间】{current_time}"""

CHAT_TEMPLATE = """
{system_prompt}

=== 对话历史 ===
{history}

=== 用户问题 ===
{user_input}

=== 你的回答 ===
{response_prefix}"""


class MultiTurnPromptBuilder:
    """多轮对话Prompt构建器"""
    
    def __init__(self, system_config: dict = None):
        self.system_config = system_config or {
            "role": "AI助手",
            "expertise": "通用领域",
            "personality": "专业、友好",
            "max_length": 500,
            "extra_rules": "",
        }
    
    def build_system_prompt(self) -> str:
        return SYSTEM_PROMPT_TEMPLATE.format(
            **self.system_config,
            current_time=__import__("datetime").datetime.now().strftime("%Y-%m-%d %H:%M"),
        )
    
    def build_messages(self, user_input: str, history: list) -> list:
        """
        构建多轮对话消息
        
        Args:
            user_input: 当前用户输入
            history: 历史对话 [(user_msg, asst_msg), ...]
        """
        messages = [{"role": "system", "content": self.build_system_prompt()}]
        
        for user_msg, asst_msg in history[-10:]:  # 最多保留10轮历史
            messages.append({"role": "user", "content": user_msg})
            messages.append({"role": "assistant", "content": asst_msg})
        
        messages.append({"role": "user", "content": user_input})
        return messages


# ============ 高级多轮对话策略 ============

def build_compressed_context(history: list, max_tokens: int = 2000) -> str:
    """
    压缩历史对话：对过长的历史进行摘要压缩
    """
    import tiktoken
    
    encoder = tiktoken.get_encoding("cl100k_base")
    
    full_history = ""
    for user_msg, asst_msg in history:
        turn = f"用户: {user_msg}\n助手: {asst_msg}\n"
        full_history += turn
    
    tokens = encoder.encode(full_history)
    if len(tokens) <= max_tokens:
        return full_history
    
    # 需要压缩：保留最近的对话，对早期的进行摘要
    recent_turns = []
    recent_tokens = 0
    half_tokens = max_tokens // 2
    
    for user_msg, asst_msg in reversed(history):
        turn_text = f"用户: {user_msg}\n助手: {asst_msg}\n"
        turn_tokens = len(encoder.encode(turn_text))
        if recent_tokens + turn_tokens > half_tokens:
            break
        recent_turns.insert(0, (user_msg, asst_msg))
        recent_tokens += turn_tokens
    
    # 早期历史用摘要替代
    early_turns = history[:-len(recent_turns)] if len(recent_turns) < len(history) else []
    
    if early_turns:
        summary = f"[早期对话摘要：共{len(early_turns)}轮，主要内容涉及{early_turns[0][0][:50]}...]\n"
    else:
        summary = ""
    
    recent_text = "\n".join([
        f"用户: {u}\n助手: {a}" for u, a in recent_turns
    ])
    
    return summary + recent_text
```

#### 2. 结构化输出Prompt设计

```python
"""
结构化输出Prompt设计
使LLM输出JSON、Markdown Table等结构化格式
"""

import json
import re
from typing import Optional

# ============ JSON模式Prompt ============

JSON_PROMPT_TEMPLATE = """你是一个数据提取助手。请从以下文本中提取信息，并以JSON格式输出。

【提取规则】
{extraction_rules}

【输出格式要求】
- 仅输出JSON，不要包含任何其他文本
- 使用如下JSON Schema：
```json
{schema}
```

【文本】
{input_text}

【JSON输出】"""


def create_json_prompt(input_text: str, schema: dict, rules: str = "") -> str:
    """创建JSON模式输出Prompt"""
    return JSON_PROMPT_TEMPLATE.format(
        extraction_rules=rules or "提取所有相关信息",
        schema=json.dumps(schema, indent=2, ensure_ascii=False),
        input_text=input_text,
    )


def parse_json_response(response: str) -> Optional[dict]:
    """从LLM响应中提取JSON"""
    # 尝试直接解析
    try:
        return json.loads(response)
    except json.JSONDecodeError:
        pass
    
    # 尝试提取代码块中的JSON
    json_match = re.search(r"```(?:json)?\s*([\s\S]*?)\s*```", response)
    if json_match:
        try:
            return json.loads(json_match.group(1))
        except json.JSONDecodeError:
            pass
    
    # 尝试提取第一个花括号对
    brace_match = re.search(r"\{[\s\S]*\}", response)
    if brace_match:
        try:
            return json.loads(brace_match.group())
        except json.JSONDecodeError:
            pass
    
    return None


# ============ 表格输出Prompt ============

TABLE_PROMPT_TEMPLATE = """请以Markdown表格格式输出以下信息。

【输出格式】
| {column_headers} |
| {separator} |
{row_template}

【信息内容】
{input}

【表格输出】"""


def create_table_prompt(input_text: str, columns: list, rows: list = None) -> str:
    """创建表格输出Prompt"""
    headers = " | ".join(columns)
    sep = " | ".join(["---"] * len(columns))
    
    return TABLE_PROMPT_TEMPLATE.format(
        column_headers=headers,
        separator=sep,
        row_template="",
        input=input_text,
    )


# ============ Function Calling / Tool Use Prompt ============

FUNCTION_CALLING_PROMPT = """你是一个可以调用工具的AI助手。请根据用户需求调用合适的工具。

可用工具：
{tool_descriptions}

调用规则：
1. 分析用户需求，判断是否需要调用工具
2. 如果需要调用，使用以下格式：
   {"function": "工具名", "arguments": {"参数名": "参数值"}}
3. 如果不需要调用工具，直接回答用户
4. 如果需要多次调用，一次只调用一个

用户：{user_input}
"""


# ============ 完整的结构化输出示例 ============

def structured_extraction_pipeline(text: str) -> dict:
    """
    结构化信息提取pipeline
    从非结构化文本中提取实体、关系、摘要
    """
    # Schema定义
    extraction_schema = {
        "type": "object",
        "properties": {
            "entities": {
                "type": "array",
                "items": {
                    "type": "object",
                    "properties": {
                        "name": {"type": "string"},
                        "type": {"type": "string", "enum": ["person", "organization", "location", "date", "product"]},
                        "mentions": {"type": "array", "items": {"type": "string"}}
                    }
                }
            },
            "summary": {"type": "string"},
            "key_points": {"type": "array", "items": {"type": "string"}},
            "sentiment": {"type": "string", "enum": ["positive", "negative", "neutral"]}
        }
    }
    
    prompt = create_json_prompt(
        input_text=text,
        schema=extraction_schema,
        rules="提取所有实体（人名、组织名、地名、日期、产品名），总结核心内容，识别情感倾向。"
    )
    
    # 实际使用时调用模型
    # response = client.chat.completions.create(model="gpt-4", messages=[{"role": "user", "content": prompt}])
    # result = parse_json_response(response.choices[0].message.content)
    
    print("Prompt已构建完成（实际使用时需调用LLM）")
    print(f"Prompt length: {len(prompt)} chars")
    return {"prompt": prompt, "schema": extraction_schema}
```

### 六、系统设计题：问答

#### Q1: 设计一个AI客服系统

**需求**：
- 支持多轮对话
- 集成企业知识库（RAG）
- 支持1000+并发用户
- P99延迟 < 3秒
- 多模型支持（复杂问题用大模型，简单问题用小模型）

**设计方案**：

**1. 系统架构**
```
[用户] -> [CDN] -> [API Gateway] -> [WebSocket/SSE]
                               |
                    [对话编排服务 (Chat Orchestrator)]
                         /          |          \
                   [意图识别]    [RAG检索]    [模型路由]
                         \          |          /
                    [LLM推理集群 (vLLM + A100)]
                         |
                    [安全审核 + 内容过滤]
```

**2. 数据流**
```
1. 用户发送消息 -> API Gateway认证限流
2. 对话编排服务获取历史
3. 意图识别 -> 判断是否需要RAG
4. 如果需要RAG -> 混合检索向量库 -> 获取上下文
5. 模型路由 -> 根据复杂度选择模型
6. 调用LLM生成回答 -> 流式返回给用户
7. 异步记录日志和Token用量
```

**3. 关键设计决策**
- 选vLLM作为推理引擎（社区活跃、功能完善）
- 使用Redis Streams做消息队列（解耦请求接收和模型推理）
- 语义缓存减少30-50%模型调用
- 小模型（7B）处理简单问答，大模型（70B）处理复杂推理
- 降级策略：大模型故障 -> 小模型 -> 缓存 -> 预设回复

---

#### Q2: 设计一个文档问答系统

**需求**：
- 支持百万级文档
- 支持PDF/Word/网页
- 高精度检索
- 文档权限控制

**设计方案**：见第09章RAG系统架构设计部分。

---

#### Q3: 设计一个Code Agent平台

**需求**：
- 用户用自然语言描述需求
- Agent自动编写代码并执行
- 支持多种编程语言
- 有安全沙箱

**设计方案**：
1. Code Agent循环：Plan -> Code -> Execute -> Debug -> Finalize
2. 安全沙箱：gVisor/Firecracker微VM隔离执行
3. 工具集：代码执行、文件操作、Git操作、测试运行
4. 执行超时控制 + 资源限制 + 敏感命令过滤

---

#### Q4: 设计一个模型网关（Model Gateway）

**需求**：
- 统一API（兼容OpenAI格式）
- 多模型路由
- Key管理 + Rate Limiting
- 成本控制 + 监控
- A/B测试能力

**设计方案**：见第09章多模型路由部分。

### 七、行为面试题

#### STAR法则回答模板

STAR = Situation（情境） + Task（任务） + Action（行动） + Result（结果）

#### 模板1: AI项目经历

```
问题：请描述一个你最引以为豪的AI项目。

Situation（情境）：
在我们的{产品名}中，用户频繁反馈回答质量不稳定，
特别是专业领域问题的准确率只有{65%}。

Task（任务）：
我负责设计和实施RAG系统来提升回答质量，
目标是准确率提高到{90%}以上。

Action（行动）：
1. 调研并选择了{混合检索 + Rerank}方案
2. 搭建了{文档解析Pipeline}，支持PDF/网页自动处理
3. 实现了{Hybrid Search}（Dense + BM25 + Rerank）
4. 设计了{语义缓存}，高频问题命中率{65%}
5. 部署了{vLLM}推理服务，支持{连续批处理}

Result（结果）：
- 回答准确率从{65%}提升到{93%}
- P99延迟控制在{2.5秒}以内
- 系统上线后支持{500+}日活用户
- 项目获得团队{最佳实践奖}
```

#### 模板2: 失败案例

```
问题：请描述一个你遇到的失败或挑战，以及如何应对的。

Situation（情境）：
在某AI客服系统上线后，发现高峰期延迟飙升，
P99延迟从{1s}暴涨到{15s}，大量用户投诉。

Task（任务）：
需要在{3天}内解决延迟问题，确保系统稳定。

Action（行动）：
1. 分析监控数据：发现的是模型推理瓶颈，{GPU显存不足}导致OOM
2. 临时方案：增加{速率限制}，降低并发请求数
3. 根本解决方案：
   - 部署{vLLM}替代原始推理方案（内存效率提升{3x}）
   - 启用{PagedAttention}和{Continuous Batching}
   - 增加{自动扩缩容}机制
   - 将大模型降级为{模型级联}（先小模型再大模型）

Result（结果）：
- P99延迟降到{2s}以下
- 系统容量从{50}并发提升到{300}并发
- 积累了经验：建立了完整的{压测流程}和{监控告警}
```

#### 模板3: 技术选型决策

```
问题：请描述一次你在技术选型中的决策过程。

Situation（情境）：
团队需要选择AI应用的推理引擎框架，候选方案包括vLLM、TGI、TensorRT-LLM。

Task（任务）：
评估并选择一个适合团队需求的推理引擎，综合考虑性能、易用性、生态。

Action（行动）：
1. 建立评估维度：{推理性能、内存效率、社区活跃度、文档完善度、部署复杂度}
2. 进行{3项基准测试}：延迟、吞吐量、显存占用
3. 对比分析：
   - vLLM：综合性能好，文档完善，社区最活跃
   - TGI：HuggingFace生态最好，但性能略低于vLLM
   - TensorRT-LLM：性能最优，但开发门槛高
4. 最终选择{vLLM}：性能足够、生态好、团队上手快

Result（结果）：
- 项目按时上线，推理性能满足SLA
- 团队{3天}内完成技术栈迁移
- 后续顺利集成了{AWQ量化}和{Prefix Caching}
```

### 八、公司面经汇总

#### 国内AI公司面试题型

| 公司 | 常见题型 | 重点考察方向 |
|------|---------|------------|
| **字节跳动** | 算法coding + 系统设计 + 项目深挖 | Attention实现、RAG架构、大模型工程化 |
| **阿里巴巴** | 八股文 + 系统设计 + 行为面试 | AI Agent、知识库系统、高并发 |
| **腾讯** | 算法 + 项目 + 场景题 | 多模态、对话系统、推理优化 |
| **百度** | 算法 + 框架源码 + 系统设计 | 文心相关、推理加速、分布式 |
| **华为** | 八股文 + 手撕代码 + 项目 | MindSpore、昇腾、模型压缩 |
| **快手** | 算法 + 场景题 + 系统设计 | 推荐系统、多模态、特征工程 |
| **美团** | 算法 + 系统设计 + 项目深挖 | 搜索推荐、知识图谱、NLP |
| **小红书** | 算法 + 项目 | RAG、内容理解、搜索 |
| **DeepSeek** | 深度技术面 + 论文讨论 | MoE、推理优化、分布式训练 |
| **智谱AI** | 技术深挖 + 算法 + 系统设计 | GLM生态、Agent、模型服务 |
| **MiniMax** | 算法 + 系统设计 + 综合面 | 对话系统、多模态、推理 |
| **月之暗面** | 技术面 + Coding + 开放题 | RAG、长上下文、Kimi架构 |

#### 海外AI公司面试题型

| 公司 | 常见题型 | 重点考察方向 |
|------|---------|------------|
| **OpenAI** | 算法hard + 系统设计 + ML基础 | Attention变体、分布式训练、推理优化 |
| **Anthropic** | 安全+对齐 + 系统设计 + 算法 | Safety、RAG、可解释性 |
| **Google** | 算法 + 系统设计 + ML基础 | TPU、Transformer、搜索架构 |
| **Meta** | 算法 + 系统设计 + 行为 | 推荐系统、LLM部署、FAISS |
| **Microsoft** | Azure + AI架构 + 算法 | 云服务集成、Copilot、推理优化 |
| **NVIDIA** | CUDA + 推理优化 + 系统设计 | TensorRT、推理加速、GPU架构 |
| **Perplexity** | 搜索 + RAG + 系统设计 | 实时搜索、RAG架构、流式处理 |
| **Scale AI** | 数据 + RLHF + 系统设计 | 数据标注、RLHF Pipeline |

#### 面试常见考点Top 10

1. **手写Attention**（基础中的基础）
2. **Transformer架构细节**（QKV、Position Encoding、LayerNorm）
3. **RAG完整流程**（文档处理 -> 索引 -> 检索 -> 生成）
4. **LLM推理优化**（PagedAttention、Flash Attention、量化）
5. **系统设计**（AI客服、RAG系统、Agent平台）
6. **模型微调**（LoRA原理、训练脚本、Q-LoRA）
7. **Prompt Engineering**（In-Context Learning、Chain-of-Thought）
8. **分布式推理**（TP/PP/EP、MoE路由）
9. **评估与监控**（Token计量、延迟追踪、A/B测试）
10. **AI安全**（内容审核、Prompt注入防御、权限控制）

### 九、复习Checklist（面试前一天冲刺）

#### 算法与代码（占总时间30%）

- [ ] 手写 Scaled Dot-Product Attention
- [ ] 手写 Multi-Head Attention
- [ ] 手写 Top-K / Top-P Sampling
- [ ] 手写 KV Cache 模拟
- [ ] 手写 Softmax（包括数值稳定性处理）
- [ ] 手写 LayerNorm / RMSNorm
- [ ] 手写 Positional Encoding (Sinusoidal / RoPE 思路)
- [ ] 手写 FeedForward Network (SwiGLU)
- [ ] 手写 Cross-Entropy Loss

#### RAG相关（占总时间20%）

- [ ] RAG完整流程（Load -> Chunk -> Embed -> Retrieve -> Generate）
- [ ] Hybrid Search实现
- [ ] Rerank原理
- [ ] 语义缓存设计
- [ ] 文档切分策略对比
- [ ] Embedding模型选型

#### 推理与部署（占总时间20%）

- [ ] vLLM / TGI / TensorRT-LLM / SGLang 对比
- [ ] 量化方案对比（AWQ / GPTQ / GGUF）
- [ ] PagedAttention原理
- [ ] Flash Attention原理
- [ ] Continuous Batching原理
- [ ] Tensor Parallel / Pipeline Parallel 原理
- [ ] 推理延迟优化策略

#### 系统设计（占总时间20%）

- [ ] RAG系统架构设计
- [ ] AI客服系统设计
- [ ] AI Agent平台设计
- [ ] 模型网关设计
- [ ] 高并发方案（请求合并、语义缓存、限流降级）
- [ ] 监控可观测性方案
- [ ] 成本优化方案

#### 行为面试（占总时间10%）

- [ ] 准备3个STAR案例（成功项目、失败案例、技术选型）
- [ ] 准备为什么离职/为什么选这家公司
- [ ] 准备职业规划
- [ ] 准备你想问面试官的问题（3-5个）

#### 公司定向准备

- [ ] 了解面试公司的核心AI产品
- [ ] 了解公司使用的技术栈
- [ ] 准备针对公司的项目经历
- [ ] 看最新的公司技术博客

#### 面试当天注意事项

- [ ] 准备1-2分钟的自我介绍
- [ ] 准备好纸笔/白板（算法题用）
- [ ] 确认是否要屏幕共享写代码
- [ ] 准备4个方向的提问问题
- [ ] 提前测试网络和麦克风

### 推荐学习资源

- **HuggingFace NLP Course**：https://huggingface.co/learn/nlp-course
- **动手学深度学习**（李沐）：https://d2l.ai
- **LLM Bootcamp**（Berkeley）
- **CS224N**（Stanford NLP）
- **CS329S**（Stanford ML系统设计）
- **The Annotated Transformer**（Harvard）
- **LLM Visualization**（bbycroft.net）
- **AIM-面试高频题库**（GitHub）
- **Awesome LLM Interview**（GitHub仓库）
